/**
 * API route registration.
 * Implemented by rosary-8n8 (read routes) and rosary-6te (write routes).
 *
 * @param {import('fastify').FastifyInstance} fastify
 * @param {{ cwd: string }} opts
 */
export async function registerRoutes(_fastify, _opts) {
  // routes registered here by rosary-8n8 and rosary-6te
}
